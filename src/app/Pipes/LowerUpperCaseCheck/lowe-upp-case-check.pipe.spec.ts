import { LoweUppCaseCheckPipe } from './lowe-upp-case-check.pipe';

describe('LoweUppCaseCheckPipe', () => {
  it('create an instance', () => {
    const pipe = new LoweUppCaseCheckPipe();
    expect(pipe).toBeTruthy();
  });
});
