import { Max10DigDirective } from './max10-dig.directive';

describe('Max10DigDirective', () => {
  it('should create an instance', () => {
    const directive = new Max10DigDirective();
    expect(directive).toBeTruthy();
  });
});
