import { DialogWithTableDataDirective } from './dialog-with-table-data.directive';

describe('DialogWithTableDataDirective', () => {
  it('should create an instance', () => {
    const directive = new DialogWithTableDataDirective();
    expect(directive).toBeTruthy();
  });
});
