import { InputWithCommaDirective } from './input-with-comma.directive';

describe('InputWithCommaDirective', () => {
  it('should create an instance', () => {
    const directive = new InputWithCommaDirective();
    expect(directive).toBeTruthy();
  });
});
